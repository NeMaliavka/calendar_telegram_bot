# 🚀 Быстрый старт с Docker

## Требования

- Docker и Docker Compose установлены
- Файл `.env` настроен
- Файл `nobugs-478214-0d41160b4771.json` присутствует

## Команды

```bash
# 1. Сборка образа
docker-compose build

# 2. Запуск всех сервисов
docker-compose up -d

# 3. Просмотр логов бота
docker-compose logs -f bot

# 4. Просмотр логов scheduler
docker-compose logs -f scheduler

# 5. Остановка
docker-compose down

# 6. Перезапуск после изменений
docker-compose restart bot
```

## Структура

- **bot** - основной Telegram бот (4GB RAM)
- **scheduler** - фоновые задачи, напоминания (2GB RAM)

## Проверка работы

```bash
# Проверить статус контейнеров
docker-compose ps

# Войти в контейнер бота
docker-compose exec bot bash

# Проверить логи
docker-compose logs --tail=100 bot
```

## Troubleshooting

Если контейнер не запускается:
1. Проверьте `.env` файл
2. Проверьте логи: `docker-compose logs bot`
3. Убедитесь, что порты не заняты
4. Проверьте права на файлы: `chmod -R 755 backend/db/chroma_db`

