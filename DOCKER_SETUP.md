# 🐳 Настройка Docker для Calendar Bot

## Быстрый старт

### 1. Подготовка

Убедитесь, что у вас есть:
- ✅ Файл `.env` с настройками
- ✅ Файл `nobugs-478214-0d41160b4771.json` (Google credentials)
- ✅ Docker и Docker Compose установлены

### 2. Сборка и запуск

```bash
# Сборка образа
docker-compose build

# Запуск контейнеров
docker-compose up -d

# Просмотр логов
docker-compose logs -f bot

# Остановка
docker-compose down
```

## Структура сервисов

### `bot` - Основной сервис бота
- Обрабатывает сообщения от пользователей
- Интеграция с Google Calendar и Sheets
- Работа с ИИ (GigaChat, RAG)
- **Память**: 4GB
- **Порт**: не требуется (использует polling)

### `scheduler` - Фоновые задачи
- Отправка напоминаний о занятиях
- Проверка завершенных уроков
- **Память**: 2GB
- **Зависит от**: `bot` (запускается после него)

## Volumes (монтируемые директории)

- `./backend` - код бота (для разработки)
- `./config` - конфигурационные файлы
- `./nobugs-478214-0d41160b4771.json` - Google credentials (read-only)
- `./backend/db/chroma_db` - векторная база данных ChromaDB
- `./logs` - логи приложения

## Переменные окружения

Все переменные загружаются из файла `.env`. Убедитесь, что он содержит:

```env
TELEGRAM_BOT_TOKEN=...
DATABASE_URL=...
SBERCLOUD_API_KEY=...
GOOGLE_CREDENTIALS_PATH=./nobugs-478214-0d41160b4771.json
# ... и другие настройки
```

## Полезные команды

```bash
# Пересобрать образ после изменений
docker-compose build --no-cache

# Перезапустить сервис
docker-compose restart bot

# Войти в контейнер
docker-compose exec bot bash

# Просмотр использования ресурсов
docker stats

# Очистка (удаление контейнеров и volumes)
docker-compose down -v
```

## Локальная PostgreSQL (опционально)

Если нужно запустить PostgreSQL локально, раскомментируйте секцию `postgres` в `docker-compose.yml`:

```yaml
postgres:
  image: postgres:15-alpine
  # ...
```

И обновите `DATABASE_URL` в `.env`:
```env
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/calendar_db
```

## Troubleshooting

### Проблема: Контейнер не запускается
```bash
# Проверьте логи
docker-compose logs bot

# Проверьте, что .env файл существует
ls -la .env
```

### Проблема: Ошибки с Google credentials
```bash
# Убедитесь, что файл существует и правильно смонтирован
docker-compose exec bot ls -la /app/nobugs-478214-0d41160b4771.json
```

### Проблема: Нехватка памяти
```bash
# Увеличьте лимиты в docker-compose.yml
mem_limit: 6g  # вместо 4g
```

### Проблема: ChromaDB не сохраняется
```bash
# Проверьте права на директорию
chmod -R 777 backend/db/chroma_db
```

## Production рекомендации

1. **Отключите монтирование кода** в `volumes` (оставьте только данные)
2. **Используйте secrets** вместо `.env` файла
3. **Настройте логирование** в файлы или внешний сервис
4. **Используйте healthchecks** для мониторинга
5. **Настройте резервное копирование** для ChromaDB и логов

