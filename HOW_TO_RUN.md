# 🚀 Как запустить бота - Пошаговая инструкция

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

**Примечание:** Если используете виртуальное окружение (рекомендуется):
```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 2: Настройка .env файла

Создайте файл `.env` в корне проекта (рядом с `run_bot.py`) со следующим содержимым:

```env
# ==== ОБЯЗАТЕЛЬНО ====
# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=ваш_telegram_id

# ==== Google Calendar (опционально, но рекомендуется) ====
GOOGLE_CALENDAR_ACTIVATE=True
GOOGLE_CREDENTIALS_PATH=./nobugs-478214-0d41160b4771.json
GOOGLE_CALENDAR_ID=nobugscoding@gmail.com

# ==== База данных PostgreSQL (опционально) ====
# Формат: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/calendar_bot

# ==== GigaChat AI (опционально) ====
SBERCLOUD_API_KEY=ваш_ключ_gigachat

# ==== База знаний (опционально) ====
CHROMA_DB_PATH=backend/db/chroma_db
PROMPT_PATH=backend/knowledge_base/documents/lor.txt
KEYWORDS_PATH=config/keywords.yaml

# ==== Рабочие часы ====
START_HOUR=9
END_HOUR=18
SLOT_DURATION=60
TIMEZONE=Europe/Moscow

# ==== Логирование ====
LOG_LEVEL=INFO
ENVIRONMENT=development
```

### Минимальная конфигурация для запуска:

Для базового запуска достаточно только:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=ваш_telegram_id
```

Остальные параметры опциональны - бот будет работать, но некоторые функции могут быть недоступны.

## Шаг 3: Запуск бота

### Вариант 1: Через run_bot.py (рекомендуется)

```bash
python run_bot.py
```

### Вариант 2: Напрямую через bot.py

```bash
python backend/bot.py
```

### Вариант 3: Через модуль Python

```bash
python -m backend.bot
```

## Что вы увидите при запуске:

```
==================================================
Запуск бота...
Режим: development
Google Calendar: Активирован
==================================================
Инициализация базы данных...
✅ База данных успешно инициализирована
Инициализация GigaChat...
✅ GigaChat успешно инициализирован
✅ Векторная база знаний успешно загружена
✅ IntentRecognizer успешно инициализирован
✅ Боковое меню команд установлено
Все обработчики зарегистрированы
[OK] Бот подключен: @your_bot_name (ID: 123456789)
Запуск polling...
```

## Остановка бота

Нажмите `Ctrl+C` в терминале, где запущен бот.

## Возможные проблемы и решения

### 1. "TELEGRAM_BOT_TOKEN не установлен"
**Решение:** Проверьте, что файл `.env` существует и содержит правильный токен.

### 2. "ModuleNotFoundError: No module named 'aiogram'"
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### 3. "DATABASE_URL не установлен" (предупреждение)
**Это нормально!** Бот будет работать, но функции БД не будут доступны.

### 4. "SBERCLOUD_API_KEY не установлен" (предупреждение)
**Это нормально!** Бот будет работать, но ИИ функции не будут доступны.

### 5. Ошибки с Google Calendar
**Решение:** 
- Проверьте путь к файлу credentials в `.env`
- Убедитесь, что календарь расшарен с email из Service Account

### 6. Ошибки с PostgreSQL
**Решение:**
- Убедитесь, что PostgreSQL запущен
- Проверьте правильность DATABASE_URL
- Убедитесь, что база данных существует

## Проверка работы бота

1. Откройте Telegram
2. Найдите вашего бота по имени
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением

## Команды бота

- `/start` - Главное меню
- `/book` или `/booking` - Записаться на пробный урок
- `/my_lessons` - Мои записи
- `/reschedule` - Перенести запись
- `/cancel` - Отменить запись
- `/faq` - Частые вопросы

## Запуск в фоновом режиме (Linux/Mac)

```bash
# Запуск в фоне с перенаправлением логов
nohup python run_bot.py > bot.log 2>&1 &

# Просмотр логов
tail -f bot.log

# Остановка
pkill -f run_bot.py
```

## Запуск как Windows Service

Для Windows можно использовать `NSSM` (Non-Sucking Service Manager) или создать `.bat` файл для автозапуска.

## Дополнительная информация

- Подробный чеклист: `STARTUP_CHECKLIST.md`
- Настройка окружения: `SETUP_ENV.md`
- Быстрый старт: `QUICKSTART.md`

