# 🚀 Быстрый старт - Calendar Bot

## Что было создано

✅ Полноценный Telegram бот на **aiogram** с интеграцией Google Calendar

### Структура проекта:
```
backend/
├── bot.py              # Основной файл бота (aiogram)
├── calendar_service.py # Работа с Google Calendar API
├── booking_service.py  # Логика бронирования
├── config.py          # Конфигурация из .env
├── utils.py           # Утилиты для кнопок
└── README.md          # Подробная документация

requirements.txt       # Зависимости Python
run_bot.py            # Скрипт для запуска
```

## Установка и запуск

### 1. Установите зависимости:
```bash
pip install -r requirements.txt
```

### 2. Настройте .env файл

Убедитесь, что в вашем `.env` файле есть:
```env
TELEGRAM_BOT_TOKEN=8401224058:AAHgif8-G4KTwEDgqtChnemAxfzuEPdUk00
GOOGLE_CALENDAR_ACTIVATE=True
GOOGLE_CREDENTIALS_PATH=./google-credentials.json
GOOGLE_CALENDAR_ID=nobugscoding@gmail.com
ADMIN_IDS=2007815494
```

### 3. Подготовьте Google credentials

У вас уже есть файл `nobugs-478214-0d41160b4771.json`. 

**Вариант 1:** Переименуйте его:
```bash
copy nobugs-478214-0d41160b4771.json google-credentials.json
```

**Вариант 2:** Или обновите `.env`:
```env
GOOGLE_CREDENTIALS_PATH=./nobugs-478214-0d41160b4771.json
```

### 4. Проверьте доступ к календарю

Убедитесь, что календарь `nobugscoding@gmail.com` расшарен с email из Service Account (из JSON файла, поле `client_email`).

### 5. Запустите бота:

```bash
python run_bot.py
```

или

```bash
python backend/bot.py
```

## Команды бота

- `/start` - Приветствие
- `/help` - Справка
- `/free` - Свободное время на сегодня (с кнопками)
- `/freeweek` - Свободное время на неделю (с кнопками)
- `/book` - Записаться на занятие

## Как работает

1. Пользователь отправляет `/book`
2. Бот показывает кнопки с свободным временем (например: "Сегодня 10:00-11:00", "Завтра 14:00-15:00")
3. Пользователь выбирает время
4. Подтверждает бронирование
5. Событие создается в Google Calendar
6. Администратор получает уведомление

## Возможные проблемы

### Бот не запускается
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`

### Ошибка доступа к календарю
- Проверьте путь к credentials файлу в `.env`
- Убедитесь, что календарь расшарен с email из Service Account
- Проверьте, что `GOOGLE_CALENDAR_ACTIVATE=True`

### Не показывает свободное время
- Проверьте рабочие часы в `.env` (START_HOUR, END_HOUR)
- Убедитесь, что в календаре есть свободные слоты

## Дополнительные настройки

В `.env` можно настроить:
```env
START_HOUR=9          # Начало рабочего дня
END_HOUR=18           # Конец рабочего дня
SLOT_DURATION=60      # Длительность слота (минуты)
TIMEZONE=Europe/Moscow # Часовой пояс
```

## Готово! 🎉

Бот готов к использованию. Все файлы созданы и настроены согласно вашему `.env` файлу.

