# 🔒 Исправление SSL ошибок

## Проблема

При работе с GigaChat API возникают SSL ошибки:
```
[SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: self signed certificate in certificate chain
```

## Решение

### 1. Отключена проверка SSL в GigaChat клиенте

В `backend/core/llm_service.py` установлено:
```python
verify_ssl_certs=False  # Отключаем проверку SSL для работы с GigaChat API
```

### 2. Добавлены ключевые слова для оплаты

В `config/keywords.yaml` добавлены ключевые слова:
- "как оплатить"
- "как оплатить курс"
- "как происходит оплата"
- "способ оплаты"
- "как платить"
- "оплата курса"
- "как заплатить"

### 3. Улучшена обработка ошибок

Добавлена специальная обработка SSL ошибок в функциях:
- `is_query_relevant_ai()` - проверка релевантности
- `get_llm_response()` - получение ответа от GigaChat

## Проверка

После перезапуска бота SSL ошибки должны исчезнуть, и запросы типа "Как оплатить курс?" должны находить правильный шаблон.

## Альтернативное решение (если проблема сохраняется)

Если SSL ошибки продолжаются, можно:

1. **Установить сертификат** `russian_trusted_root_ca.cer` в корень проекта
2. **Использовать прокси** без SSL проверки
3. **Настроить переменные окружения**:
   ```env
   REQUESTS_CA_BUNDLE=""
   CURL_CA_BUNDLE=""
   ```

## Отключение телеметрии PostHog

Телеметрия PostHog (ChromaDB) отключена через:
```env
ANONYMIZED_TELEMETRY=False
```

Если ошибки PostHog продолжаются, они не критичны и не влияют на работу бота.

