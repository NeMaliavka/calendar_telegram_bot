# 📊 Настройка Google Таблиц для записи бронирований

## Описание

Бот автоматически записывает все бронирования, отмены и переносы в Google Таблицу для удобного отслеживания и анализа.

## Структура таблицы

Таблица автоматически создает следующие колонки:

1. **Дата создания** - когда была создана запись в боте
2. **Дата и время занятия** - когда запланировано занятие
3. **Имя** - имя пользователя
4. **Telegram username** - username пользователя в Telegram
5. **Telegram ID** - уникальный ID пользователя
6. **Телефон** - телефон (если указан)
7. **Статус** - Создана / Отменена / Перенесена
8. **ID события** - ID события в Google Calendar
9. **Ссылка на событие** - прямая ссылка на событие в календаре
10. **Примечание** - дополнительная информация (например, новое время при переносе)

## Настройка

### 1. Создайте Google Таблицу

1. Перейдите на [Google Sheets](https://sheets.google.com)
2. Создайте новую таблицу
3. Назовите лист (например, "Записи") или оставьте "Sheet1"
4. Скопируйте ID таблицы из URL:
   ```
   https://docs.google.com/spreadsheets/d/ВАШ_ID_ТАБЛИЦЫ/edit
   ```

### 2. Предоставьте доступ Service Account

1. Откройте вашу таблицу
2. Нажмите кнопку **"Настройки доступа"** (Share)
3. Добавьте email из вашего Service Account JSON файла
   - Откройте файл `nobugs-478214-0d41160b4771.json` (или другой credentials файл)
   - Найдите поле `client_email` (например, `service-account@project.iam.gserviceaccount.com`)
   - Добавьте этот email с правами **"Редактор"** (Editor)

### 3. Настройте .env файл

Добавьте следующие переменные в ваш `.env` файл:

```env
# Google Sheets
GOOGLE_SHEETS_ACTIVATE=True
GOOGLE_SHEETS_ID=ваш_id_таблицы_из_url
GOOGLE_SHEETS_NAME=Записи
```

**Пример:**
```env
GOOGLE_SHEETS_ACTIVATE=True
GOOGLE_SHEETS_ID=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
GOOGLE_SHEETS_NAME=Записи
```

### 4. Перезапустите бота

После настройки перезапустите бота. При первом бронировании таблица автоматически создаст заголовки.

## Как это работает

- **При создании бронирования**: В таблицу добавляется новая строка со статусом "Создана"
- **При отмене**: Статус записи обновляется на "Отменена" с датой отмены
- **При переносе**: 
  - Старая запись получает статус "Перенесена" с примечанием о новом времени
  - Создается новая запись со статусом "Перенесена" и ссылкой на новое событие

## Проверка работы

1. Создайте тестовое бронирование через бота
2. Откройте вашу Google Таблицу
3. Убедитесь, что появилась новая строка с данными

## Отключение записи в таблицу

Если вы хотите временно отключить запись в таблицу, установите в `.env`:

```env
GOOGLE_SHEETS_ACTIVATE=False
```

Бот продолжит работать с календарем, но не будет записывать данные в таблицу.

